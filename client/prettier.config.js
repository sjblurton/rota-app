//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: true,
  quoteProps: 'as-needed',
  arrowParens: 'always',
  printWidth: 100,
  tabWidth: 2,
  endOfLine: 'lf',
  trailingComma: 'all',
}

export default config
